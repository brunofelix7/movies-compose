package dev.brunofelix.movies.core.data.local.preferences

/**
 * Centralized file for all DataStore preference keys.
 */
object PreferencesKeys {
    /**
     * The name of the DataStore file.
     */
    const val DATASTORE_NAME = "user_preferences"

    /**
     * Key for the user's preferred language (e.g., "en", "pt-BR").
     */
    const val LANGUAGE_KEY = "language_key"
}
